<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="myapp.your_flashcards">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_colour_flashcard"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_colour_flashcard"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Flashcard.Flashcard_Activity"
            android:label="@string/title_activity_flashcard_"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>